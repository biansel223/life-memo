{
  "name": "life-memo",
  "version": "1.0.0",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "npm ci --omit=dev --cache .npm-cache --no-audit --no-fund"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}